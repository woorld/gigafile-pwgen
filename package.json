{
  "name": "gigafile-pwgen",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run remove && tsc && npm run copy",
    "copy": "node -e \"require('fs-extra').copySync('./src/static', './dist')\"",
    "remove": "node -e \"require('fs-extra').removeSync('./dist/')\""
  },
  "author": "woorld",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.235",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "fs-extra": "^11.1.1"
  }
}
